const path = require('path')

module.exports = {
    'config': path.resolve('./src', 'config.json'),
    'migrations': path.resolve('./src', 'migrations.'),
    'models-path': path.resolve('./src', 'modles'),
    'seeders-path': path.resolve('./src', 'seeders')
}